# AI Gateway — Alembic configuration.
# The database URL is resolved at runtime from settings (see migrations/env.py),
# derived from DATABASE_URL with the async driver swapped for a sync one.

[alembic]
script_location = migrations
prepend_sys_path = .
