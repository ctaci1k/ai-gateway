---
plan: 011-rag
status: done
updated: 2026-05-29
---

# RAG — документи, embeddings, vector search

> Реалізує [09-roadmap](../09-roadmap.md) Фазу 6 (день 11) **повноцінно, без костилів**: upload → chunking → embeddings → vector search → інʼєкція контексту, + RAG-режим в UI. Будується на [007](007-persistence-database.md) + дизайні (006).

## СТАН (читається першим)
- Останній виконаний крок: **7** — усі кроки + DoD виконані; фаза PH10 завершена.
- Наступний крок: — (перехід до PH11, план [005](005-tooling-and-quality-gates.md) §005b).
- Заблоковано: ні.
- Змінені файли: BE — `requirements.txt` (chromadb/pypdf/python-multipart), `core/config.py` (RAG-налаштування), `.env.example`, `db/models.py` (`Document`), `migrations/versions/0004_documents.py`, `core/rag/{__init__,embeddings,parser,chunking,store}.py`, `services/rag_service.py`, `schemas/documents.py`, `routes/documents.py`, `main.py`, `schemas/chat_schema.py` (`rag_enabled`), `schemas/chat_response.py` (`rag_enabled`/`rag_sources`/`RagSource`), `services/orchestrator_service.py` (rag_context), `routes/chat.py`, `prompts/prompts.yaml` (`rag_augmented`, v2), `tests/{conftest,test_rag}.py`. FE — `services/{documentsApi,ragApi}.ts`, `services/apiClient.ts` (FormData), `store/RagContext.tsx`, `store/ChatModeContext.tsx` (`rag`), `components/rag/{DocumentsPanel,RagSources}.tsx`, `features/rag/{RagPage,useRag}.ts(x)`, `components/sidebar/ChatModeSelector.tsx`, `components/icons/Icons.tsx` (`IconDoc`/`IconUpload`), `app/{layout,page}.tsx`, `i18n/messages/{en,uk,pl}.json`, `theme/components.css`, `types/api.ts`. Docs — `03`, `04`, `08`. `.gitignore` (chroma_data).
- Відкриті питання/рішення: embeddings — Gemini `gemini-embedding-001` (абстраговано через `EmbeddingClient`).

## Дефолти
- Vector DB: **ChromaDB** (персистентний), шлях/конфіг із `.env`.
- Embeddings: **Gemini embeddings** (вже є `google-generativeai`), ключ із `.env`; провайдер абстрагувати (легко замінити).
- Парсинг: **PyPDF** (+ txt). Ліміти розміру/типу файлу.

## Кроки
- [x] 1. Залежності: `chromadb`, `pypdf`, `python-multipart`, embeddings (Gemini); `.env.example` (шлях Chroma, ліміти).
- [x] 2. Пайплайн інжесту: parse → chunk → embed → store (метадані per-user; `documents` + ChromaDB).
- [x] 3. Ретрив: similarity search + інʼєкція контексту в промпт відповідачів (`rag_augmented`, v2).
- [x] 4. Ендпоінт `/documents` (upload/list/delete) з валідацією типу/розміру/кількості (формат помилок з 002).
- [x] 5. RAG-режим в оркестраторі (`rag_context`/`rag_enabled`) — узгоджено з режимами чату.
- [x] 6. FE: upload-UI + RAG-режим у `ChatModeSelector`, `RagPage`+`DocumentsPanel`+`RagSources`, i18n, a11y, стани.
- [x] 7. Тести: інжест, ретрив, релевантність, ізоляція per-user (+ парсинг/чанкінг, API).

## Перевірка (Definition of Done)
- [x] Upload → запитання → відповідь, ґрунтована на документі (retrieval працює).
- [x] Vector store персистентний; ізоляція per-user; ліміти файлів.
- [x] FE: upload + RAG-режим, усі стани + i18n + a11y; без `fetch` у компонентах.
- [x] Тести зелені; контракти/докси оновлені; план `status: done`.
