# 10 — Журнал рішень (ухвалені)

> Раніше тут були **відкриті** суперечності. Власник продукту делегував рішення архітектору й підтвердив напрям: **документація приводиться під поточну реальну програму**, а первісний концепт залишається історією + джерелом майбутніх фіч.
> Рішення нижче — **ухвалені** й діють як контракт. Дата: 2026-05-29.

Легенда: ✅ ухвалено · ↪ як застосовано.

---

## D-1 ✅ Моделі-відповідачі = поточна реальність

**Рішення:** канон — **Groq (`llama-3.3-70b-versatile`) + Cerebras + SambaNova** як відповідачі, **Gemini (`gemini-2.5-flash-lite`) — суддя**. GPT/Claude з первісного концепту — **опційні майбутні провайдери**, не наявні.
↪ Усі документи вже відображають це. Додавання GPT/Claude — лише через процедуру «Як додати провайдера» ([05](05-providers-and-selector.md)).

## D-2 ✅ Зараз — один користувач; пізніше — *мінімальні* акаунти

**Рішення:** поточний режим — однокористувацький. У майбутньому — **мінімальні акаунти/логін** (легкі, без enterprise-auth): логін відмикає збереження чатів і синхронізацію персоналізації.
↪ Код залишає місце під ключ користувача; повноцінний auth не будуємо до відповідної фази ([09](09-roadmap.md), Фаза 5).

## D-3 ✅ Single — ефемерний; збереження — окрема майбутня фіча

**Рішення:** одинарний чат (Single) **не зберігається як іменований чат** (підтверджено власником). «Збереження чатів (до 3)» — окрема фіча, прив'язана до акаунтів, і стосується збережених (Compare) чатів, а не Single.
↪ Поточний дефект (Single пише в `ChatBuffer`) **виправити** у Фазі 0 ([09](09-roadmap.md)). Збережені чати — Фаза 4.

**Уточнення (PH13, 2026-05-29):** «ефемерність» Single тепер **візуальна, не на рівні БД**. Ходи Single **записуються в rolling-історію** (`interactions`) після стріму — для персоналізації; це **не** створює збережений (Compare) чат. UI має кнопку «Очистити», що прибирає лише **візуальний** тред (дані в БД лишаються). Збережені чати лишаються **виключно** для Compare; у Single секції збережених чатів немає.

## D-4 ✅ RAG / БД / Деплой — ПІДТВЕРДЖЕНО як майбутній обсяг

**Рішення:** це **потрібні** фічі (не «можливо»), але **після стабілізації ядра**. Відповідають дням 10–14 первісного плану, яких ще не зроблено (останній коміт — «after day 9»).
↪ Внесено як зобов'язані фази в [09-roadmap.md](09-roadmap.md). Принцип: не починати RAG поверх ядра з боргом.

## D-5 ✅ Формат помилок API

**Рішення:** перейти з «HTTP 200 + `{"error":...}`» на **коректні статус-коди** (400/422/500…) з уніфікованим тілом:
```json
{ "error": { "code": "string", "message": "string" } }
```
↪ Фаза 0 ([09](09-roadmap.md)); оновити обробку на фронтенді.

## D-6 ✅ Невживані налаштування селектора

**Рішення:** **реалізувати** `SELECTOR_MAX_RETRIES`, `SELECTOR_MIN_CONFIDENCE`, `SELECTOR_TEMPERATURE`, `SELECTOR_MAX_TOKENS` (бо якість суддівства нам потрібна), а не видаляти.
↪ Фаза 3 ([09](09-roadmap.md)).

## D-7 ✅ Фронтенд: поточна структура — норма, TS — поступово

**Рішення:** зафіксувати наявну структуру (без `src/`, змішані `.js/.jsx/.ts`) як канон. Нову візуальну/архітектурну основу власник схвалив. Міграція на TypeScript — поступова (нові файли `.ts/.tsx`), без великого переписування.
↪ Відображено в [06-frontend-architecture.md](06-frontend-architecture.md).

---

## D-8 ✅ Повний рефактор + усі фічі, безперервно, 100%, без боргу

**Рішення (2026-05-29, розширено власником):** виконуємо **абсолютно все** — рефактор **і** раніше відкладені фічі — **з чистого листа, нуль технічного боргу, нуль костилів, нічого «на потім»**. Програма: плани **001–012** (фази PH0–PH12), безперервно одним агентом за runbook [plans/EXECUTE.md](plans/EXECUTE.md); на незалежних кроках — паралелізм субагентів (по 3–4) з верифікацією.
↪ **Обсяг включає:** БД-персист (007), акаунти/auth (008), збереження чатів ≤3 (009), промпти/YAML (010), RAG (011), деплой-артефакти (012), дизайн pixel-match (006, ungated).
↪ **Скасовує «відкладеність»** D-2/D-4: ці можливості тепер у активному обсязі. Принципи з них лишаються чинними (мінімальні акаунти; Single ефемерний — D-3).
↪ **Єдина зовнішня залежність (не борг):** реальне підняття VPS, DNS і випуск SSL потребують інфри/секретів власника; агент створює всі артефакти + owner-handoff ([plans/012-deployment.md](plans/012-deployment.md)).

## D-9 ✅ Суддя — на Groq (Gemini лишається для embeddings)

**Рішення (PH13, 2026-05-29):** перенести **AI-суддю** з Gemini на **Groq**, нейтральною моделлю `qwen/qwen3-32b`. Причина: безкоштовний тариф Gemini обмежує `generate_content` до **20 запитів/добу** на `gemini-2.5-flash-lite` — суддя постійно падав у rule-based fallback. Groq дає ~60/хв і ~14 400/добу. Це **уточнює D-1** (де суддею був Gemini).
↪ `SELECTOR_PROVIDER=groq`, `SELECTOR_MODEL=qwen/qwen3-32b` (`config/selector_config.py`); суддя будується окремо від відповідачів (`ProviderService.build_judge`), тож **немає self-bias** (qwen ≠ жоден з відповідачів groq/cerebras/sambanova). `SELECTOR_MAX_TOKENS=2048` — запас для reasoning + JSON. **Gemini лишається для RAG-embeddings** (окрема, набагато більша квота). Ручний перевибір **оборотний** — можна погодитись із вибором судді назад.

## D-10 ✅ Адмінпанель + ліміти акаунтів + реєстрація за кодом (план 015)

**Рішення (2026-05-29):** власник дає доступ працедавцям і хоче спостерігати/обмежувати їхні запити, щоб не вичерпати безкоштовні API-ключі.

- **Модель ліміту = кількість запитів.** Compare (мульти) рахується як **1 запит**, Single = 1 запит.
- **Per-user ліміти у БД:** `max_requests_per_minute`, `max_requests_per_day`. Дефолт працедавцям: **5/хв, 30/день**. **Адмін: null/null = безліміт.**
- **Безліміт (null) → банер обмеження НЕ показується** і enforcement не застосовується.
- **Адмін** — прапорець `is_admin` (доступ до адмінпанелі + безліміт). Адмін-акаунт визначається через `ADMIN_USERNAME` (.env).
- **Реєстрація лише за кодом** `REGISTRATION_CODE` (.env): без валідного коду — `403 invalid_registration_code`. Працедавцям акаунти створює адмін у панелі (там же ліміти).
- **Аудит:** окрема append-only таблиця `usage_events` (не обрізається, на відміну від rolling `interactions`) — для лічби лімітів і перегляду в адмінці; зберігає витрачені токени.
↪ Реалізація — [plans/015-admin-and-quotas.md](plans/015-admin-and-quotas.md). Контракти → `03-api-contracts.md` (admin, register code, `429 quota_exceeded`), `04-data-models.md` (`usage_events`, нові поля `User`).
↪ **Реалізовано (PH15, 2026-05-30):** усі пункти вище в коді й покриті тестами (BE `test_admin_quotas.py`; FE адмінпанель/банер/реєстрація). Деталі — [08-current-state.md](08-current-state.md) (секція PH15). Гейти зелені: BE 101 passed + ruff/black; FE tsc/eslint/prettier/vitest(12)/build.

## D-11 ✅ Поліш чатів + правдиві моделі + навчання судді (план 016)

**Рішення (2026-05-30):** друга партія недоробок (A–F4). Уточнює D-1 (лінап) і D-9 (суддя).

- **(B) Правдиві назви моделей — єдине джерело правди.** Реєстр `config/models_config.py` (`ModelSpec{api_model_id, display_name, max_tokens}`); `api_model_id` із `.env`. Назви правдиві скрізь (код, `/providers/info`, UI, `.env(.example)`), без вендорської брехні. **Не** змушуємо моделі вигадувати ідентичність.
- **(OD-1) Cerebras = `zai-glm-4.7`** (GLM-4.7). Замінює слабкий `gpt-oss-120b`. Рекомендована `llama-4-scout-17b-16e-instruct` **недоступна** в API Cerebras (live-звірка) → обрано найсильнішу доступну й відмінну. Reasoning-модель → `CEREBRAS_MAX_TOKENS=4096` (глобально `RESPONDER_MAX_TOKENS=2048`).
- **(OD-2) SambaNova = `DeepSeek-V3.1`** (відмінна від Groq-Llama і Cerebras-GLM). Кандидат `DeepSeek-R1-Distill-Llama-70B` недоступний; `DeepSeek-V3.1` доступний і дає повний контент. Лінап: Llama / GLM / DeepSeek — три по-справжньому різні; суддя Qwen не збігається з жодним.
- **(E) Суддя лишається Qwen**, але вчиться на ручних виборах: `manual_model_selections` у промпт + анти-позиційне впорядкування + **обмежене** пост-зваження (`selector/preference_weighting.py`; зміщує лише near-ties, ніколи не перекриває явно кращу; прозоро в `selector_metadata.preference_weighting`).
- **(OD-3) Ліміт = 25 збережених чатів** (`SAVED_CHATS_LIMIT`, BE+FE синхронно). При досягненні — явне повідомлення «Ліміт 25 чатів досягнуто. Видаліть чат, щоб створити новий» (i18n uk/pl/en). **Без автовидалення.**
- **(A/C/D/F) UX:** Compare — чернеткове «+» (без персисту до 1-го повідомлення), автозбереження + назва за 1-м повідомленням, завжди повний тред, без лічильника; Single — підтвердження+очищення треду при зміні моделі (стан у store); тонка тематична смужка скролу через токени.
↪ **Реалізовано (PH16, 2026-05-30):** усе вище в коді + тести. Деталі — [08-current-state.md](08-current-state.md) (секція PH16); контракти — [03-api-contracts.md](03-api-contracts.md) (`display_name`, `preference_weighting`); провайдери/суддя — [05-providers-and-selector.md](05-providers-and-selector.md). Гейти: BE 113 + ruff/black; FE tsc/eslint/prettier/vitest(12)/build.

## D-12 ✅ Виправлення чатів + живі ліміти + BYOK (план 017)

**Рішення (2026-05-30):** третя партія — 2 баги + живі ліміти + BYOK (свої ключі). Уточнює D-10 (квоти) і D-1/D-9/D-11 (провайдери/суддя).

- **(A) Видалення чату** гарантовано видаляє `chat_messages`; одноразова чистка сиріт на старті; `PRAGMA foreign_keys=ON` (SQLite). Новий чат завжди порожній.
- **(B) Картка недоступної** показує реальну назву моделі (динамічно/BYOK-override), залитий пунктир, читабельний текст.
- **(C) Живі ліміти:** хвилина — вікно «від 1-го запиту» (60с, повний скид через `minute_resets_in_seconds`); день — **календарний за Europe/Warsaw**, скид о 00:00 польського часу. Банер — два рядки, безлімітні приховані, хвилинний тикер; `/auth/me` розширено (`remaining_this_minute`, `minute_resets_in_seconds`, `day_resets_at`). Enforcement = ті самі вікна.
- **(BYOK) Свої API-ключі** для судді + 3–5 відповідачів, **доступно всім типам акаунтів**. **Безпека (критично):** ключі **НЕ** в БД і **НЕ** в логах — лише `sessionStorage` браузера + транзит у кожному запиті; бекенд використовує in-memory й відкидає. У проді — обов'язково HTTPS. Валідація на «Зберегти» (живий тест-виклик, пер-ключові помилки, неробочі лишаються у формі). Назви моделей у UI = введений `model_id`. **Квоти:** Single безкоштовний на своєму ключі; Compare безкоштовний лише коли **всі** учасники (відповідачі + суддя) на своїх; адмін — без лімітів, але з BYOK. Compare адаптивний під 4–5; зелена плашка (свій ключ) / червона (частковий набір). Модель судді обирається і в Single (NQ6).
↪ **Реалізовано (PH17, 2026-05-30):** усе вище в коді + тести (`test_byok.py`, оновлені `test_chats.py`/`test_admin_quotas.py`/`test_response_selector.py`/`test_chat_stream.py`). Деталі — [08-current-state.md](08-current-state.md) (секція PH17); контракти — [03-api-contracts.md](03-api-contracts.md) (`/auth/me`, `byok`, `/keys/validate`); дані — [04-data-models.md](04-data-models.md) (`ByokConfig`, вікна); провайдери — [05-providers-and-selector.md](05-providers-and-selector.md) (транзитні BYOK). Гейти: BE 123 + ruff/black; FE tsc/eslint/prettier/vitest(12)/build.

## D-13 ✅ Фінальний UX-поліш (план 018)

**Рішення (2026-05-31):** завершальна партія дрібних, але важливих UX-правок після PH17. Уточнює D-12 (BYOK-квоти/тексти) і D-10/D-12 (ліміти).

- **Білінг моделі судді в Single = Варіант A.** На **своєму** ключі наші ліміти **не списуються** взагалі; списуємо **лише** за використання **вбудованих** AI (ключі застосунку). Вбудований суддя (Qwen) у Single **не пропонується**; своя модель у полі «Суддя» з'являється чіпом у Single з позначкою «ваша / без обмежень». Узгоджує суперечність текстів D-12 (прибрано фразу «…і лише якщо це не суддя»).
- **Ліміти власного ключа невидимі нам.** Коли провайдер віддає rate-limit/quota на BYOK-моделі — показуємо **окреме**, грамотне повідомлення («ліміти вашого ключа вичерпано — перевірте акаунт провайдера»), чітко відмінне від нашого `quota_exceeded`. Реалізація: `classify_provider_failure` → `reason` у `failed_providers[]` (Compare) і в стрім-події `{type:"error",reason}` (Single).
- **Ім'я автора — латиницею в усіх мовах:** «**Stanislav Byndas**».
- **Хвилинний ліміт — повний скид:** після завершення фіксованого хвилинного вікна (від 1-го запиту) лічильник повертається **повністю** до ліміту (напр. одразу 5), а не +1 поштучно. Фіксоване вікно, узгоджене між enforcement і відображенням (`QuotaService._minute_window`, реконструкція з `usage_events`).
- **Решта (UX):** тематичний тонкий скролбар у вікні BYOK; банер лімітів без переповнення (pl/uk); спільна кнопка «вниз» + авто-скрол (Single+Compare); спрощена картка акаунта (іконка + ім'я по центру + «Вийти», без підпису).
↪ **Реалізовано (PH18, 2026-05-31):** усе вище в коді + тести (`test_admin_quotas.py` — фіксоване вікно; `test_chat_stream.py` — reason у стрім-помилці). Деталі — [08-current-state.md](08-current-state.md) (секція PH18); контракти — [03-api-contracts.md](03-api-contracts.md) (вікно, стрім-`reason`); провайдери — [05-providers-and-selector.md](05-providers-and-selector.md) (ліміт власного ключа). Гейти: BE 128 + ruff/black; FE tsc/eslint/prettier/vitest(12)/build.

## D-14 ✅ BYOK-мітки «AI 1/2/3» + мова за умовчанням EN (план 019)

**Рішення (2026-05-31):** партія дрібних UX-правок після PH18. **Уточнює D-11** (правдиві назви), не скасовує його.

- **BYOK-мітки відповідачів — узагальнені, ЛИШЕ у вікні «свої ключі».** Три дефолтні відповідачі в `KeysModal` показуються як **«AI 1/2/3»** з підписом **«типово: <реальна модель>»** (Llama 3.3 70B / GLM-4.7 / DeepSeek V3.1), дзеркалячи рядок судді «типово: Qwen». **Скрізь інде** (Compare-картки, перемикач Single, банер судді, `/providers/info`) назви лишаються **правдивими** — D-11 чинне. Обсяг підтверджено власником: «лише вікно BYOK».
- **Картка акаунта:** ім'я поряд з іконкою (по центру відносно іконки), не по центру всього рядка; «Вийти» праворуч. (Коригує PH18/7.)
- **Перемикач мов на екрані входу** (раніше був лише в сайдбарі) — спільний `LanguageToggle` для обох місць.
- **Мова за умовчанням = English** для нових користувачів: `navigator.language` більше не авто-визначається; вибір зберігається в `localStorage` і відновлюється.
↪ **Реалізовано (PH19, 2026-05-31):** усе вище в коді; деталі — [08-current-state.md](08-current-state.md) (секція PH19). Гейти: FE tsc/eslint/prettier/vitest(12)/build; бекенд не змінювався. **Поза обсягом:** валідація доданої BYOK-моделі (порожні→видалити, невалідні→червоним) — окремий майбутній план.

## D-15 ✅ BYOK: життєвий цикл доданої моделі (план 020)

**Рішення (2026-05-31):** як поводяться **додані (custom) моделі** у вікні «свої ключі». Продовжує D-14.

- **Зберігається лише валідна+робоча додана модель.** Порожні та невалідні custom-рядки **не** потрапляють у `sessionStorage`.
- **Порожній** custom-рядок на «Зберегти» → автоматично **видаляється** (і візуально, і не персиститься).
- **Невалідний** custom-рядок на «Зберегти» → лишається **червоним** у відкритому вікні (щоб виправити друкарську помилку), але не зберігається; при закритті вікна — зникає (сховище лишається чистим).
- Кнопка прибирання custom-рядка — **«Видалити»** (без змін).
- Підпис custom-рядків — **«Додана модель — AI 4/5»** (продовження нумерації дефолтних AI 1/2/3, D-14).
- Дефолтні 3 слоти (groq/cerebras/sambanova) + суддя — **без змін** (порожнє=вбудований; невалідне=червоним для виправлення).
↪ **Реалізовано (PH20, 2026-05-31):** `KeysContext.buildPersistedState`/`sanitizeLoadedState` (+юніт-тести), `KeysModal` (prune порожніх на Save + підпис), i18n `keys.customResponderNumbered`. Деталі — [08-current-state.md](08-current-state.md) (секція PH20). Бекенд не змінювався. Гейти: FE tsc/eslint/prettier/vitest(16)/build.

## D-16 ✅ Правдиві назви BYOK + очищення ключів при виході + мобільна версія/згортання (план 023)

**Рішення (2026-05-31):** партія UX+безпека після PH22. **Уточнює D-11/D-14** (правдиві назви) і **D-12** (BYOK-безпека), нічого не скасовує.

- **(A) Правдиві назви скрізь.** У Compare перевизначений дефолтний слот (свій ключ на groq/cerebras/sambanova) показує введений `model_id`, не реєстрову назву. Назва судді (топбар + банери) = реальний BYOK-суддя, коли активний; інакше дефолтний (Qwen). Жодного хардкоду, що ігнорує BYOK.
- **(B) Безпека.** Logout / зміна акаунта в тій самій вкладці **очищає** BYOK-ключі (`sessionStorage` + стан) — новий/інший акаунт ніколи не успадковує чужі ключі. Відновлення сесії того ж юзера (reload) ключі **зберігає**.
- **(C/D) Згортання + статуси (ChatGPT-стиль).** Сайдбар згортається на компʼютері (вузький рейл з кольоровими квадратиками) і має бургер-шухляду на телефоні. Квадратики — **лише** у згорнутому: обмежений акаунт=червоний, частковий набір=червоний (окремий), свій ключ=зелений; «SB» (автор), «API» (BYOK); мова ховається. Статуси — **єдина система** (один `useSidebarStatus`), без дублювання логіки. Банер лімітів переноситься донизу, не вилазить за сайдбар.
- **(E) Повний responsive** під телефон (360/390/430px): без горизонтального скролу, Compare у 1 колонку, діалоги на всю ширину, таргети ≥44px.
↪ **Реалізовано (PH23, 2026-05-31):** усе вище в коді + тести (`store/KeysContext.test.ts` +6, усього 22 FE). Деталі — [08-current-state.md](08-current-state.md) (секція PH23); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md) (сайдбар: згортання/шухляда/статуси). Бекенд не змінювався. Гейти: FE tsc/eslint/prettier/vitest(22)/build.

## D-17 ✅ Редизайн «Classic Console» + Single-чати зберігаються + реальні Налаштування (план 024)

**Рішення (2026-06-02):** велика візуальна переробка під референс **«AI Gateway · Classic Console»** + функційні зміни. **Переписує D-3** (Single більше не «ефемерний як іменований чат») і уточнює D-1/D-9 (промпт судді).

- **(Single-персистентність — переписує D-3)** Single-чати тепер **зберігаються** як іменовані (назва = текст 1-го повідомлення; **прив'язані до однієї моделі**, зафіксованої при створенні; окремий список історії). Таблиця `chats` отримала `mode` ('single'|'compare', дефолт 'compare' для легасі) і `model` (слот для Single; NULL для Compare) — Alembic `0006`. `/chat/stream` приймає `chat_id` і **персистить хід** у `chat_messages` (дзеркалить `/chat`). Уточнення PH13 (rolling-`interactions` для персоналізації) **лишається чинним**. Без `chat_id` Single нічого не зберігає у `chats` (іменований Single створюється явно через `POST /chats`).
- **(Ліміти НЕ чіпали)** Request-квота лишається **сумарною** на Single+Compare (рішення власника 2). Ліміт **збережених** чатів (`SAVED_CHATS_LIMIT=25`) — **спільний** на обидва режими.
- **(Зміна моделі)** Модель фіксується при створенні Single-чату; у наявному чаті клік по чипу показує підказку «змінити модель можна лише в новому чаті» — **без діалогу й без очищення** (прибрано теперішній ConfirmDialog-флоу `ModelSwitcher`).
- **(Реальні Налаштування)** Екран Settings (модалка з розділами): **Критерії суддівства** — редагування з «Скинути до типового» + показ типового; персист **per-user** у `Preference.data['judge_prompt_override']` (без міграції), бекенд застосовує при суддівстві (`build_selector_prompt`). **API-ключі (BYOK)** — перенесено наявний функціонал (`KeysForm`, без дублювання логіки) у розділ Settings.
  - **Уточнення (2026-06-02):** редагованими лишаються **лише критерії суддівства** (на що зважати), а не весь промпт. Механічний каркас — роль-замок, правила вибору, контракт виводу (JSON-only, `$scores_example`, **шкала 0–100**, бал для кожної моделі, `selected_model` з дозволених), підстановки даних — **фіксований системно** (шаблон `selector_judge` з плейсхолдером `$judging_criteria`; дефолтні критерії — `selector_judge_criteria`). `judge_prompt_override` тепер = **критерії** (вільний текст, без вимоги плейсхолдерів), інжектиться значенням у каркас (один прохід підстановки → користувач не може зламати парсинг/шкалу чи інжектити плейсхолдери). Валідація спрощена до ліміту довжини. Причина: прибирання «0–100» чи формату з повного промпта **погіршувало** суддівство й частіше зривало у rule-based fallback. Гейти: BE 161 + ruff/black; FE tsc/eslint/prettier/vitest(26)/build.
- **(Заглушки «в розробці»)** Профіль і Аватар, Безпека (меню акаунта), Звіти (кнопка для **всіх**, у топ-барі) — спільна `ComingSoonModal`.
- **(Chrome за enterprise-конвенцією)** Новий топбар (бренд, тема moon/sun, мова-дропдаун, usage-пілюля, Settings/Admin icon-btn, акаунт-дропдаун); акордеонний сайдбар (Single Models / Compare з New Chat + History, відносний час); картка творця. **BYOK → у Settings**; **ліміти/usage → топ-індикатор** + composer-notice; сайдбар чистий (замінює PH23-рейл/квадратики; мобільну бургер-шухляду збережено).
- **(Увесь наявний функціонал лишився)** прикріплення файлів (RAG), ручний перевибір Compare, банер судді, картки провайдерів/невдач, правдиві назви BYOK (PH23/A), очищення ключів при logout (PH23/B).
↪ **Реалізовано (PH24, 2026-06-02):** код + тести (BE `test_chats.py` мода/модель/Single-persist, `test_judge_prompt.py`; FE `utils/relativeTime.test.ts`). Деталі — [08-current-state.md](08-current-state.md) (секція PH24); контракти — [03-api-contracts.md](03-api-contracts.md) (chats `mode`, `/chat/stream chat_id`, `/preferences/judge-prompt`); дані — [04-data-models.md](04-data-models.md) (`chats.mode/model`, `judge_prompt_override`); суддя — [05-providers-and-selector.md](05-providers-and-selector.md) (override промпта); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md) (Classic Console chrome, Settings, історія Single).

## D-18 ✅ Звіти про використання + `usage_events` як канонічний per-turn ledger (план 025)

**Рішення (2026-06-02):** власник делегував архітектору реалізувати розділ **«Звіти»** — повну self-service історію активності акаунта (запити, токени, моделі, чати, повідомлення) «як велика досвідчена корпорація». **Уточнює D-10** (квоти/`usage_events`) і **D-12/D-17** (BYOK/чати), нічого не скасовує.

- **`usage_events` стає канонічним per-turn ledger.** Один рядок = один хід (Compare = 1 подія). Додано `chat_id` (FK→chats, **ondelete SET NULL** — аудит переживає видалення чату, відв'язуючись у групу «видалені/ad-hoc»), `billable` (списувалось із квоти застосунку), `token_estimated` (токени — оцінка). Alembic `0007`. **Не обрізається.**
- **Пишемо ВСІ ходи, у т.ч. BYOK** (раніше BYOK не записувався → не було у звіті). **Квотні вікна рахують лише `billable=true`** — поведінка лімітів НЕ змінюється (BYOK і далі безкоштовний), але реєстр повний. Це **обов'язковий сумісний рефактор** (інакше запис BYOK почав би списувати квоту).
- **Токени — чесно.** Пріоритет — реальні (provider `usage`): Compare вже мав; Single-стрім — `stream_options.include_usage` + читання фінального chunk. Фолбек — евристика `ceil(chars/4)` (`core/tokens.py`, без нової залежності) з прапором `token_estimated` (UI: «~» + бейдж «оцінка»). tiktoken — поза обсягом.
- **Звіти — окремий per-user `/reports/*`** (auth, **НЕ** admin-gated — Звіти для всіх, D-17/п.6), агрегації read-only в `memory/usage_report_repository.py`; per-user ізоляція сувора. Дашборд із вкладками (Огляд/За моделями/За чатами/Журнал), фільтр періоду, легкі inline-SVG графіки (без важких залежностей), серверний CSV-стрім.
- **Кнопка «Звіти» (для всіх) перестає бути заглушкою** (`ComingSoonModal("reports")` → реальний `ReportsModal`).
↪ **Реалізовано (PH27, 2026-06-02):** код + тести (BE `test_reports.py` 12 кейсів + `test_tokens.py`; FE — vitest 26, i18n-паритет). Деталі — [08-current-state.md](08-current-state.md) (секція PH27); контракти — [03-api-contracts.md](03-api-contracts.md) (`/reports/*` + `events.csv`); дані — [04-data-models.md](04-data-models.md) (ledger-колонки); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md) (дашборд). Гейти: BE 157 + ruff/black; FE tsc/eslint/prettier/vitest(26)/build. **Опційно (не зроблено):** адмін-перегляд звітів іншого юзера (бонус G).

↪ **UX-апгрейд (PH28, 2026-06-02, Варіант B):** власник: дашборд у модалці «дрібний/незручний». Архітектор виніс Звіти у **повноекранний розділ** (як Admin), додав вкладку **«Розкладка»** з акордеон-drill-down **ключ доступу → модель → чати** і **глобальний фільтр за ключем доступу** (`access=app|own` на всіх `/reports/*` + новий `GET /reports/breakdown`). «Ключ доступу» — фільтр/факт, не рівень-секція (його місце — сегмент тулбара і верхній рівень drill-down). План — [plans/026-reports-fullpage-breakdown.md](plans/026-reports-fullpage-breakdown.md); деталі — [08-current-state.md](08-current-state.md) (секція PH28). Гейти: BE 159 + ruff/black; FE tsc/eslint/prettier/vitest(26)/build.

## D-19 ✅ BYOK «API-ключі»: курований Select base URL + суддя base URL/Clear + ⓘ-підказки (план 027)

**Рішення (2026-06-02):** власник: «незрозуміло, звідки брати base URL — незручно». Архітектор переробив UX BYOK. **Уточнює D-12/D-15/D-16** (BYOK), нічого не скасовує; безпека D-12 збережена.

- **Base URL = курований Select, не вільний рядок.** Список перевірених OpenAI-сумісних endpoint'ів (вбудовані Groq/Cerebras/SambaNova + сумісні OpenAI/OpenRouter/Together/Fireworks/DeepSeek/Mistral/xAI/Gemini-OpenAI/Perplexity/Ollama) + «Власний…» (текстове поле як fallback). Єдина FE-константа `utils/byokEndpoints.BYOK_BASE_URLS` (дзеркало бекендових дефолтів). **Anthropic НЕ додаємо** — його API не chat/completions-сумісне через цей механізм (потрібен окремий адаптер → окремий план).
- **Дефолтні слоти + суддя** мають опцію «Типовий ендпоінт» (→ порожній `baseUrl`, бекенд бере фіксований). **Суддя отримав base URL** (`JudgeKey.baseUrl`; раніше поля не було) — дефолт «Вбудований суддя (Groq)»; кнопка **«Очистити»** скидає key/model/baseUrl → вбудований Qwen на Groq. Кастомні AI 4/5 — лишили **«Видалити»**.
- **ⓘ-підказки** (спільний a11y `InfoTip`) на base URL / API-ключ / ID моделі: де взяти ключ, що таке ID моделі, парність із base URL. Тексти-нотатки переписані під реальні поля.
- **Бекенд без змін** — уже приймав `base_url` будь-якого слота і судді (PH22; `KeyValidateEntry`+`is_judge`, `ByokJudge`, `build_transient_judge`).
↪ **Реалізовано (PH29, 2026-06-02):** код (`utils/byokEndpoints.ts`, `components/keys/BaseUrlSelect.tsx`, `components/common/InfoTip.tsx`, `KeysForm`, `KeysContext`, i18n, CSS) + оновлені тести `store/KeysContext.test.ts` (judge.baseUrl + legacy-нормалізація). Деталі — [08-current-state.md](08-current-state.md) (секція PH29); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md). Гейти: FE tsc/eslint/prettier/vitest(27)/build; бекенд не змінювався.

↪ **Ревізія PH29.1 (фідбек власника, Варіант A):** власник: вбудовані endpoint'и не мають бути **у списку** (до них лише скидання), вільний текст прибрати, неповний рядок не зберігати — але **base URL лишається на всіх рядках як SELECT**. Тому **base URL — `BaseUrlSelect` (вибір зі списку) на Судді + AI 1/2/3 + кастомних**; каталог = **лише сумісні провайдери** (без вбудованих у списку, без «Власний…»/вільного тексту). Вбудовані слоти: base URL **необов'язковий** (порожньо=вбудований endpoint), override через вибір сумісного, **«Очистити»** (на всіх трьох + суддя) = скид. Кастомні AI 4/5: base URL обов'язковий + «Видалити». **Неповний рядок не зберігається** (бланк/дроп + inline-підказка); повний built-in слот зберігає свій override. **Свідомо прибрано** вільний текст base URL і вбудовані провайдери зі списку. Безпека D-12 збережена. Гейти: FE tsc/eslint/prettier/vitest(29)/build; бекенд без змін.

↪ **Ревізія PH29.2 (фідбек власника):** неповний рядок блокує «Зберегти» (не тихий «успіх»). Якщо на слоті обрано endpoint без ключа+моделі (або заповнено частково) — Save **не валідує/не персистить/не показує «Збережено»**, а підсвічує рядок червоним із повідомленням і лишає його незмінним (endpoint не викидається). Хелпери `findIncompleteSlots`/`isBuiltinIncomplete`/`isCustomIncomplete` у `KeysContext` (+тести). Гейти: FE tsc/eslint/prettier/vitest(34)/build; бекенд без змін.

## D-20 ✅ BYOK: серверне зашифроване per-account зберігання (реверс D-12) + model discovery + довідник ключів (план 028)

**Рішення (2026-06-03):** власник делегував архітектору «зробити як велика корпорація». **Свідомо РЕВЕРСУЄ D-12** у частині «ключі ніколи не в БД» (інші принципи D-12 — «не логуємо», валідація на Save — лишаються чинними). Уточнює D-15/D-16/D-19 (BYOK UX).

- **Реверс D-12 → server-side, per-account, ЗАШИФРОВАНО.** BYOK-ключі тепер зберігаються в БД (`byok_credentials`), щоб переживали різні пристрої/сесії: зайшов під тим самим логіном з іншого комп'ютера — ключі на місці; інший акаунт бачить **лише свої** (ізоляція по `user_id`); вийшов — у браузері нічого не лишається (чиститься лише локальний кеш метаданих, серверні ключі лишаються per-account). Транзит ключа — **тільки** на момент збереження (`PUT /keys`) і валідації/discovery; чат їх більше **не передає** (`ChatRequest.byok` ігнорується — клієнт не може інжектити ключі, безпековий плюс).
- **Чому не zero-knowledge.** Гейтвей викликає провайдерів **на сервері** (Compare/суддя/стрім) → мусить уміти розшифрувати ключ у момент використання. Справжній zero-knowledge неможливий; ми свідомо беремо **custody** ключів під server-side envelope encryption.
- **Криптографія = AES-256-GCM (AEAD) envelope** (`core/secret_box.py`). KEK — з env `BYOK_ENCRYPTION_KEY` (base64, 32 байти); кожен запис — випадковий 96-біт nonce; зберігаємо `ciphertext`+`nonce`+`key_version`. **AAD = `f"{user_id}:{slot}"`** прив'язує шифротекст до власника+слота (підміна рядка в БД провалить GCM-тег). **Плейнтекст ключа й KEK — ніколи в БД і ніколи в логах.** Рівні зрілості (чесно): *Good* — KEK у env-секреті (наш рівень на VPS); *Better* — per-user DEK під KEK; *Ideal* — KEK у KMS/Vault. `key_version` готує ротацію. **Втрата/зміна KEK = усі збережені ключі недійсні** (користувачі вводять заново).
- **Write-only UX.** `GET /keys` повертає лише метадані (`slot`/`base_url`/`model_id`/`last4`/`custom`) — повний ключ **ніколи** не віддається. UI: маска «••••last4 — введіть, щоб замінити»; Save шле лише змінені рядки (збережений ключ переюзається при зміні model/base_url); Clear/Remove → `DELETE /keys/{slot}`. Усі здобутки PH29.x (Select base URL, ⓘ-підказки, блокування неповних рядків, жива валідація на Save) збережено.
- **Model discovery (новий UX).** Поле «ID моделі» — комбобокс (`POST /keys/models` → `GET {base_url}/models` через сервер) з реальними моделями акаунта. 3 запобіжники: typeahead (нативний `<datalist>`), евристичний фільтр чат-моделей + «показати всі», і **фолбек на ручний ввід** (нативний `<input list>` ніколи не блокує). Discovery вміє листати і за **вже збереженим** ключем (decrypt server-side).
- **Довідник ключів (без лого).** Глобальна кнопка «Де взяти ключі та моделі?» (`ProviderGuide`) + контекстний лінк «Отримати API-ключ ↗» у ⓘ біля поля ключа за провайдером рядка. Дані лінків — в одному джерелі (`utils/byokEndpoints.ts`). Зовнішня навігація лише на доку провайдера — **жодні ключі/дані не передаються**.
↪ **Реалізовано (PH30, 2026-06-03):** код (`core/secret_box.py`, `db/models.py::ByokCredential` + Alembic `0008`, `memory/byok_repository.py`, `routes/keys.py` CRUD+`/models`, `routes/chat.py` вантажить із БД; FE `services/keysApi.ts`, `store/KeysContext.tsx`, `components/keys/{KeysForm,ModelCombobox,ProviderGuide,BaseUrlSelect}.tsx`, `utils/byokEndpoints.ts`) + тести (`tests/test_secret_box.py`, оновлений `tests/test_byok.py`; FE `store/KeysContext.test.ts`). Деталі — [08-current-state.md](08-current-state.md) (секція PH30); контракти — [03-api-contracts.md](03-api-contracts.md) (`/keys` CRUD, `/keys/models`, прибраний транзит); дані — [04-data-models.md](04-data-models.md) (`byok_credentials`, envelope); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md). **Owner-action:** завести `BYOK_ENCRYPTION_KEY` у прод-секрети. Гейти: BE 178 + ruff/black; FE tsc/eslint/prettier/vitest(29)/build.

↪ **Ревізія PH30.2 (2026-06-03, фідбек власника):** поле моделі стало **select-only** (вибір зі списку discovery, без вільного вводу). Обов'язковий запобіжник плану («ніколи не блокувати, фолбек на ручний ввід») збережено через **розумний фолбек за HTTP-кодом** `/models`: 404 (`no_models`, напр. Perplexity) і порожній список при валідному ключі → відкривається ручний ввід; **401/403 (`bad_key`) ручне НЕ відкривають** — показ «перевірте ключ» (поганий ключ більше не розблоковує вільний ввід). Плюс ревізія текстів розділу API-ключів під реальність PH30/PH30.1 (зокрема прибрано хибне «ключ ніколи не зберігається»). Бекенд-контракт `/keys/models` без змін, лише точніший `error_reason` (`classify_discovery_failure`). План — [plans/029-byok-model-select-and-texts.md](plans/029-byok-model-select-and-texts.md); деталі — [08-current-state.md](08-current-state.md) (PH30.2).

↪ **Ревізія PH30.1 (2026-06-03, фідбек власника — усунення нестиковок UX):** (1) виправлено суперечливий текст (`keys.intro` казав «ключі лише у вкладці браузера» всупереч серверному шифруванню); (2) слоти підписано провайдером — «AI 1 · Groq» / «AI 2 · Cerebras» / «AI 3 · SambaNova»; (3) **частковий реверс PH29.1:** вбудовані провайдери повернено у випадайку Base URL — у кожному слоті його **власний** провайдер є **іменованим дефолтом** («Groq · типовий ендпоінт»), а **два інші вбудовані + сумісні** доступні як override-опції (список тепер збігається з довідником «Де взяти ключі»). Власник: «для кожного селекту один вбудований = типовий, інші показуються як у довідці». Бекенд не змінювався. Деталі — [08-current-state.md](08-current-state.md) (PH30.1).

## D-21 ✅ Звіти: атрибуція моделі до джерела ключа (маска display-only, розділення за `(model, key)`) (план 030)

**Рішення (2026-06-03):** власник: у Звітах біля ID моделі писати, ЧИМ вона викликана — «Вбудована» (app-ключ) чи модель + **маска свого ключа** `перші4••••останні4` (напр. `gsk_••••OTzu`). **Уточнює D-18** (Звіти/`usage_events`) і **D-20** (BYOK-сховище), нічого не скасовує.

- **Маска = display-only; шифрування лишається справжнім.** У БД ключ і далі зберігається зашифровано (AES-256-GCM envelope, `byok_credentials`, D-20). Маска рахується з **плейнтексту в момент запису ходу** (плейнтекст уже є в розшифрованому `ByokConfig`) і **денормалізовано** пишеться в нову колонку `usage_events.key_fingerprint` (Alembic `0009`). Денормалізація навмисна: звіт показує, яким ключем користувалися **на той момент**, навіть якщо ключ згодом змінили/видалили. **Плейнтекст ключа — ніколи в логах/відповідях/ledger.** first4 — публічний префікс (`gsk_`/`sk-`…), last4 — нетаємний (вже в write-only UI); обидва бачить **лише власник** (per-user ізоляція Звітів).
- **`key_fingerprint` NULL = вбудована (app-ключ); set = свій ключ.** Атрибуція за **обраною** моделлю ходу: Compare — слот-переможець (`selected_model`) серед `byok.responders`; Single — judge-слот або responder за `request.provider`.
- **Розділення за `(selected_model, key_fingerprint)`.** Усі агрегати «за моделлю» (`by_model`, вузол моделі в `breakdown`, журнал/CSV) групуються за парою. Та сама модель: вбудована (NULL) і своя (маска) → **різні рядки**; різні свої ключі → теж різні. `selected_model` **не змінюється** (маска — окремий розрізнювач). Верхній рівень `breakdown` `access_key` (billable, PH28/D-18) — без змін.
- **Свідомо поза обсягом:** правдиве ім'я моделі для BYOK-override на вбудованому слоті (показуємо назву слота + маску) — окремий майбутній план. Квоти/orchestrator/`access`-фільтр/інші роути — не чіпалися; нового env/owner-action нема.
↪ **Реалізовано (PH31, 2026-06-03):** код (BE `db/models.py`, `memory/byok_repository.py::key_fingerprint`, `memory/usage_repository.py`, `memory/usage_report_repository.py`, `routes/chat.py::_selected_key_fingerprint`, `routes/reports.py`, `schemas/reports.py`, `migrations/0009`; FE `types/api.ts`, `components/reports/{KeyBadge,reportUtils,ByModelTab,BreakdownTab,ActivityLogTab}`, `i18n`, `theme/components.css`) + тести (`test_byok.py`, `test_reports.py`). Деталі — [08-current-state.md](08-current-state.md) (PH31); контракти — [03-api-contracts.md](03-api-contracts.md); дані — [04-data-models.md](04-data-models.md); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md). Гейти: BE 187 + ruff/black; FE tsc/eslint/prettier/vitest(29)/build.

## D-22 ✅ Правдиве ім'я моделі скрізь (BYOK-aware): денормалізована реальна модель + самоописний хід (план 031)

**Рішення (2026-06-03):** власник делегував архітектору «зробити як велика корпорація». **Уточнює D-21** (атрибуція ключа) і **D-18** (Звіти/ledger), нічого не скасовує. Закриває свідомо відкладену в D-21 межу — «правдиве ім'я моделі для BYOK-override на вбудованому слоті».

- **Розділення понять `slot` vs `model`.** `selected_model` (і `chats.model`) **лишається слотом** — стабільний групувальник; квоти/персоналізація/судова логіка/`access`-фільтр на нього зважають і **не чіпаються**. Реальну модель несе **нова денормалізована колонка** `usage_events.model_name` (String(128), nullable; Alembic `0010`) — повторює перевірений патерн D-21/`key_fingerprint` (мінімальний вибух: одна колонка + одна міграція). Compare: `all_responses[selected_model]["model"]`; Single: модель зі стріму (`selected_model` вирівняно під слот = `request.provider`). `NULL` для історичних рядків → FE безпечний фолбек на лейбл слота. **Без backfill** старих Single-рядків (слот недетермінований із api-id заднім числом) — задокументована межа.
- **Самоописний хід (`is_byok` per-response).** Кожна відповідь у `all_responses` та кожен failed-провайдер несуть `is_byok` (`isinstance(provider, TransientProvider)`) + реальну `model` (також на невдачі). Адитивні поля → persist у `chat_messages.payload` «безкоштовно». Завдяки цьому **replay** Compare (банер переможця + картки) показує правду зі **збереженого** ходу, **не** з поточних ключів.
- **Єдиний FE-хелпер `modelDisplay(slot, model, isByok)`** — `isByok ? (model || responderLabel(slot)) : responderLabel(slot)`. Усі історичні/звітні поверхні (звіти `by_model`/`breakdown`/`activity`/`by_chat`, банер, картки, адмін-аудит) переходять на нього. **Композерні/active-model поверхні (`MainHead`/`SingleModelPicker`/`ModelSwitcher`/`sidebarStatus`) свідомо лишаються на поточних ключах** (`byokModelId`) — вони відображають, ЧИМ ти слатимеш **зараз**; це коректно, **не люк**.
- **Групування звітів** додає `model_name` третім виміром у `by_model`/`breakdown` (та сама модель/ключ із різними реальними моделями → різні рядки; built-in `model_name` стабільний → не фрагментує).
- **Безпека/межі:** плейнтекст ключа й KEK — ніколи в логах/відповідях/ledger; шифрування БД (D-20, AES-256-GCM) не чіпане; звіти суворо per-user; квоти/orchestrator-логіка вибору — без змін. Нового env/owner-action нема.
↪ **Реалізовано (PH32, 2026-06-03):** код (BE `db/models.py`, `migrations/0010`, `memory/usage_repository.py`, `memory/usage_report_repository.py`, `routes/chat.py`, `routes/reports.py`, `services/provider_service.py`, `services/admin_service.py`, `schemas/{chat_response,reports,admin}.py`; FE `utils/models.ts`, `components/reports/{reportUtils,ByModelTab,ByChatTab,BreakdownTab,ActivityLogTab}`, `components/selector/SelectorBanner`, `components/compare/{CompareTurn,CompareColumn,CompareFailedCard,CompareModal}`, `features/compare/rows.ts`, `components/admin/AdminPanel`, `types/api.ts`) + тести (`test_byok.py`, `test_provider_service.py`, `test_reports.py`, `test_admin_quotas.py`; FE `utils/models.test.ts`, `SelectorBanner.test.tsx`). Деталі — [08-current-state.md](08-current-state.md) (PH32); контракти — [03-api-contracts.md](03-api-contracts.md); дані — [04-data-models.md](04-data-models.md); провайдери — [05-providers-and-selector.md](05-providers-and-selector.md); фронтенд — [06-frontend-architecture.md](06-frontend-architecture.md). Гейти: BE 196 + ruff/black; FE tsc/eslint/prettier/vitest(35)/build.

## D-23 ✅ UX-поліш + баги B1–B6 (+B3b): мітки Compare, плашка власної моделі, мова відповіді, приглушена світла тема, авторитетні критерії судді, дедуп discovery (план 032)

**Рішення (2026-06-03):** власник дав партію конкретних UX-правок і багів; делегував архітектору реалізувати «без напівфіч». **Уточнює** D-13/D-22 (плашка active-model на поточних ключах), D-17/D-11 (критерії/нудж судді), D-12/D-18 (reason-коди провайдерів). Безпеку D-20/D-21/D-22 не чіпано; квоти/`selected_model`/судова логіка вибору переможця — без змін. Нового env/owner-action нема; міграцій нема.

- **(B1) Мітки Compare — лише текст.** Прибрано «3/три/three/trzy» з `compare.modeTag` («Порівняння · моделі + суддя») і `topbar.compareInfo`. Самі моделі/роутинг Compare не чіпано. Паритет uk/pl/en.
- **(B2) Плашка «ваша модель» — лише власний ключ.** Single-чип (`MainHead`) і картка `SingleModelPicker` показують фіолетову плашку (як прапор переможця Compare, **без рамки**) лише коли модель на власному ключі (`byokModelId(slot) !== null`, покриває responder + judge); built-in (app-ключ) — без плашки. Це **active-model** поверхня → свідомо на поточних ключах (D-22, не люк).
- **(B3a) Робастний парс контенту (Mistral).** Відповідач читає контент із запасних reasoning-полів (`reasoning_content`/`reasoning`) у повному й стрім-режимі; порожнє через ліміт класифікується окремо (`length_exceeded`, output truncated) проти `empty_response`. Mistral більше не дає глухого «empty response» — або відповідає, або дає **чітку локалізовану причину**. Контракт `failed_providers[].reason` розширено (новий код), наявні коди не зламані.
- **(B3b) Мова відповіді = мова повідомлення.** Відповідач відповідає **мовою повідомлення користувача** (фолбек — UI-локаль uk/pl/en). Інжектиться як директива у промт **відповідача** (Single і Compare); суддю/ембеддинги/квоти не чіпано. UI-локаль передається опційним `ChatRequest.locale`.
- **(B4) Світла тема — приглушений off-white + глибина.** Лишено білий (не жовтий/не сірий): muted off-white canvas, білі поверхні «плавають» над фоном (інверсія шарування темної теми), крихкіші межі. Правлено лише light-гілку `theme/tokens.css`; темну тему не чіпано.
- **(B5) Критерії судді авторитетні.** Каркас `selector_judge` підсилено: бали МУСЯТЬ відображати критерії користувача; явно кращий за критеріями → суттєво вищий бал. Нудж (`preference_weighting`) — лише тайбрейкер на справжній near-tie і **ніколи** не перекриває кращу за критеріями; за наявності `judge_prompt_override` нудж **вимкнено** (criteria authoritative).
- **(B6) Дедуп моделей discovery.** `ModelCombobox` дедуплює список за нормалізованим `id` (trim, case-insensitive, лишає перший) на рівні **даних** → чисті `<select>`-опції й стабільні ключі (без React-warning).
↪ **Реалізовано (PH33, 2026-06-03):** код (FE `components/keys/ModelCombobox.tsx`, `components/layout/MainHead.tsx`, `components/chat/SingleModelPicker.tsx`, `components/compare/CompareFailedCard.tsx`, `store/ComposerContext.tsx`, `features/compare/useCompare.ts`, `services/{chatApi,compareApi}.ts`, `types/api.ts`, `theme/{tokens,components}.css`, `i18n`; BE `providers/openai_compatible.py`, `services/{provider_service,orchestrator_service}.py`, `selector/{preference_weighting,response_selector}.py`, `routes/chat.py`, `schemas/{chat_schema,chat_response}.py`, `core/prompts.py`, `prompts/prompts.yaml`) + тести (BE `test_responders.py`/`test_provider_service.py`/`test_prompts.py`/`test_preference_weighting.py`/`test_response_selector.py`; FE `ModelCombobox.test.ts`). Деталі — [08-current-state.md](08-current-state.md) (PH33). Гейти: BE 207 + ruff/black; FE tsc/eslint/prettier/vitest(39)/build.

## D-24 ✅ UX-поліш 2 + атрибуція ключа B7–B11: write-only ключ, читабельні CTA, тематичний скрол, текст автора, дата-кероване built-in/own, BYOK-суддя у звітах (план 033)

**Рішення (2026-06-03):** власник дав партію UX-правок + атрибуцію ключа; делегував архітектору «без напівфіч». **B12 скасовано власником.** Уточнює D-21/D-22 (атрибуція ключа/правдива модель) і D-18 (Звіти/ledger); безпеку D-20/D-21/D-22 не чіпано (шифрування BYOK, маски display-only, slot-vs-model). Квоти/`billable`/`selected_model`/winner-логіка/one-row-per-turn — без змін.

- **(B7) Кнопка show/hide API-ключа — прибрана.** Ключі write-only (серверне зашифроване сховище, D-20) → показувати нема чого; поле — звичайний `<input type="text">` з маскою-плейсхолдером для збереженого слота.
- **(B8) Залиті-акцентом CTA у попапах — читабельний hover/active.** Інверсія filled→outline (поверхня `--panel` + акцентний текст/рамка `--accent`) в обох темах, замість старого `filter:brightness`, через який білий текст «зникав». Єдине правило для `dialog-btn--primary`/`admin-btn--primary`/`auth-submit`.
- **(B9 — ядро) built-in vs own-key СКРІЗЬ дата-кероване.**
  - **(a)** Перевірено емпірично (TestClient з реальним AES-шифруванням): write-path **уже коректний** для всіх own-key випадків (Single custom AI4/5, Single суддя-слот, Compare-переможець built-in-слот/custom) — `key_fingerprint` завжди маска, `null` лише для built-in. **Фіксу не потрібно.** Залишкове «Вбудована» на own-key можливе **лише на історичних рядках до Alembic 0009** (NULL, плейнтексту для backfill нема — задокументована межа D-21).
  - **(b)** **Доданий (BYOK) суддя видимий у статистиці навіть у Compare.** Дві денормколонки `usage_events.judge_model_name`/`judge_key_fingerprint` (nullable; Alembic `0011`), заповнюються лише коли суддя на власному ключі (вбудований → NULL, не показуємо). Звіти **синтезують похідний** judge-рядок (`by_model` `role="judge"`, реальна модель + маска, токени=0 — не подвоюють переможця; прихований під app-фільтром, бо суддя own-key) і judge-вузол під «own» (`breakdown`); журнал/CSV несуть judge-колонки inline. **Рядок переможця лишається канонічним one-per-turn; суддя — похідний рядок зі своїх денормколонок.** Квоти/`billable` — не зламано.
  - **(c)** **Аудит хардкоду (BE+FE): 0 багів.** Усі звітні/історичні поверхні дата-керовані (за збереженим ходом); active-model поверхні (`MainHead`/`SingleModelPicker`/`sidebarStatus`) свідомо на поточних ключах (D-22, не люк).
  - **Безпека:** плейнтекст ключа й KEK — ніколи в логах/відповідях/ledger; маски display-only.
- **(B10) Тематичний тонкий скрол скрізь.** Глобальне правило на токенах (Firefox на `html` + WebKit `*::-webkit-scrollbar*`) замість per-class allowlist → усі контейнери послідовні.
- **(B11) Текст автора повернено.** Картка автора рендерить `author.tagline`: «Full-Stack Developer — зроблено повністю самотужки, від початку до кінця» (без «Junior»/«AI orchestration»); прибрано мертвий `author.rights`. Паритет uk/pl/en.
↪ **Реалізовано (PH34, 2026-06-03):** код (FE `components/keys/KeysForm.tsx`, `components/sidebar/CreatorCard.tsx`, `components/reports/{ByModelTab,BreakdownTab,ActivityLogTab}.tsx`, `types/api.ts`, `theme/components.css`, `i18n`; BE `db/models.py`, `migrations/0011_usage_judge_byok`, `memory/{usage_repository,usage_report_repository}.py`, `routes/{chat,reports}.py`, `schemas/reports.py`) + тести (BE `test_byok.py` +3, `test_reports.py` +6). Деталі — [08-current-state.md](08-current-state.md) (PH34); контракти — [03-api-contracts.md](03-api-contracts.md); дані — [04-data-models.md](04-data-models.md). **Owner-action:** нема нового env; **є Alembic-міграція `0011`** (авто на деплої). Гейти: BE 216 + ruff/black; FE tsc/eslint/prettier/vitest(39)/build.

## Нові підтверджені вимоги (від власника, 2026-05-29)

Окрім днів 10–14, додано до обсягу:

- **Логування / спостережуваність** (нове) — структуровані логи запитів, помилок, рішень судді.
- **Мінімальні акаунти / логін** (нове, легкий варіант) — див. D-2.
- **Збереження чатів (до 3)** — див. D-3.
- **RAG** — день 11.

Усе це внесено в [09-roadmap.md](09-roadmap.md). Жодних відкритих блокерів наразі немає.
