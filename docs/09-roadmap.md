# 09 — Дорожня карта (зобов'язана)

> ⚡ **Статус: у активному виконанні.** Раніше відкладені фази (БД, акаунти, чати, RAG, деплой) **переведені в обсяг повного рефактору** (рішення D-8) і сплановані як виконавчі плани `docs/plans/007–012`. Канонічний порядок і прогрес — у [plans/EXECUTE.md](plans/EXECUTE.md). Цей файл лишається стратегічним описом «навіщо/що»; «коли/як» — у `plans/`.

Замінює первісний «14-денний план» (він був навчальним і змішував Python-теми + продукт). Тут — **продуктовий** план під поточну реальну програму.

**Статус первісного плану:** дні **1–9 виконані** (останній коміт «after day 9»: до User Preferences включно). Дні **10–14 — попереду** і **потрібні** (рішення D-4). Нижче вони змаплені на фази + додані нові вимоги власника (логування, акаунти, збереження чатів).

> Головний принцип: **спершу стабілізувати ядро** (Compare + Selector), потім нарощувати. Не починати RAG/деплой поверх ядра з боргом.

Легенда: 🔧 борг · 🆕 нова вимога · 📅 день первісного плану.

---

## Фаза 0 — Стабілізація ядра 🔧 (≈ 📅 День 12: FastAPI production) — ПРІОРИТЕТ

Виправити дефекти з [08-current-state.md](08-current-state.md):

- [ ] Помилки → коректні HTTP-коди + уніфіковане тіло `{error:{code,message}}` (D-5). (#2)
- [ ] CORS: явний список origins, прибрати `*`+credentials. (#1)
- [ ] Single зробити ефемерним: прибрати запис у пам'ять (D-3). (#3)
- [ ] API base URL фронтенду → `.env`. (#5)
- [ ] Стабілізувати парсинг відповіді судді (structured output / валідна схема). (#6)
- [ ] Базова валідація + ліміти запитів.

## Фаза 1 — Логування / спостережуваність 🆕

- [ ] Структуровані логи: вхідні запити, помилки провайдерів, рішення судді (обраний model, confidence, fallback).
- [ ] Єдиний формат логів; рівні (info/warn/error). Дешево й допомагає всім наступним фазам — тому рано.

## Фаза 2 — Керування промптами + YAML (📅 День 10)

- [ ] Винести промпт селектора з коду у `prompts.yaml` (PyYAML).
- [ ] Версіонування промптів.

## Фаза 3 — Якість суддівства (D-6)

- [ ] Реалізувати `SELECTOR_MAX_RETRIES`, `SELECTOR_MIN_CONFIDENCE`, `SELECTOR_TEMPERATURE`, `SELECTOR_MAX_TOKENS`.
- [ ] Видимий fallback-банер в UI (недоступність Gemini).
- [ ] Вивести в UI метрики згоди/незгоди із суддею (лічильники вже є в `ChatBuffer`).

## Фаза 4 — Збереження чатів (до 3) + персист персоналізації (D-3)

- [ ] Легкий персист (JSON-файл або легка БД) для `user_preferences` (щоб не зникали при рестарті).
- [ ] Бекенд чатів: створити / перемкнути / видалити / перейменувати; ліміт **до 3** (тестова версія).
- [ ] Підключити наявні UI-компоненти (`ChatList` тощо) до реального API.
- [ ] Стосується збережених (Compare) чатів; Single лишається ефемерним.

## Фаза 5 — Мінімальні акаунти / логін 🆕 (D-2)

- [ ] Легка автентифікація (без enterprise-auth): логін відмикає збережені чати + синхронізацію персоналізації.
- [ ] Ізоляція даних per-user; міграція однокористувацького стану під ключ користувача.

## Фаза 6 — RAG (📅 День 11)

- [ ] Завантаження PDF/документів → chunking → embeddings → vector search → інʼєкція контексту.
- [ ] Vector DB (ChromaDB або FAISS).
- [ ] Режим RAG у `ChatModeSelector` (місце вже передбачене у фронтенді).

## Фаза 7 — Деплой (📅 День 13)

- [ ] Dockerfile + docker-compose.
- [ ] Nginx reverse proxy + SSL (Let's Encrypt) + домен.
- [ ] Ubuntu VPS, публічний доступ. (Можна підняти staging раніше за потреби.)

## Наскрізне — Тести (📅 День 14) + якість

- [ ] Тести (pytest): ChatBuffer, selector, агрегація, API — **писати з Фази 0**, не лишати на кінець.
- [ ] Rate limiting / захист від спаму.
- [ ] Документація оновлюється разом із кодом (docs = джерело правди).

---

### Послідовність (стисло)

`Фаза 0 (стабілізація) → 1 (логи) → 2 (промпти/YAML) → 3 (якість судді) → 4 (чати+персист) → 5 (акаунти) → 6 (RAG) → 7 (деплой)`, тести наскрізно.

> Усе з первісного концепту, що потрібно (дні 10–14), збережено й розподілено. Нічого не загублено; «mock interview» з дня 14 прибрано як навчальний елемент, не продуктовий.
